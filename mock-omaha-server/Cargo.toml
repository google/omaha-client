# Copyright 2024 The Fuchsia Authors
#
# Licensed under a BSD-style license <LICENSE-BSD>, Apache License, Version 2.0
# <LICENSE-APACHE or https://www.apache.org/licenses/LICENSE-2.0>, or the MIT
# license <LICENSE-MIT or https://opensource.org/licenses/MIT>, at your option.
# This file may not be copied, modified, or distributed except according to
# those terms.

[package]
name = "mock-omaha-server"
version = "0.4.0"
edition = "2024"
description = "Mock implementation of the server end of the Omaha Protocol"
license = "BSD-2-Clause OR Apache-2.0 OR MIT"
repository = "https://github.com/google/omaha-client"
readme = "README.md"

exclude = [".*"]

[features]
default = ["tokio"]
tokio = ["dep:tokio", "hyper-util/tokio", "hyper-util/client-legacy"]

[dependencies]
anyhow = "1.0"
argh = "0.1"
derive_builder = "0.20"
hex = "0.4"
http-body = "1"
hyper = { version = "1", default-features = false, features = ["http1", "server"] }
hyper-util = "0.1"
log = "0.4"
omaha_client = { version = "0.4", path = "../omaha-client" }
p256 = "0.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
url = "2"

[target.'cfg(not(target_os = "fuchsia"))'.dependencies]
tokio = { version = "1", features = ["rt", "net", "sync", "time", "io-util", "macros", "rt-multi-thread"], optional = true }

[target.'cfg(target_os = "fuchsia")'.dependencies]
tokio = { version = "1", features = ["rt", "sync", "time", "io-util", "macros", "rt-multi-thread"], optional = true }

[dev-dependencies]
hyper-util = { version = "0.1", features = ["http1"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(fasync)'] }
